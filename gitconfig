[user]
    name = Anton Kolesov
    email = anthony.kolesov@gmail.com
[alias]
    b = branch
    br = branch
    bv = branch -v
    bra = branch -av
    ci = commit
    cia = commit --amend
    ciaa = commit --amend -a
    co = checkout
    cp = cherry-pick
    cpc = cherry-pick --continue
    cpa = cherry-pick --abort
    rb = rebase
    rbi = rebase -i --autosquash
    rbc = rebase --continue
    rbs = rebase --skip
    rba = rebase --abort
    log-summary = log --date=short --pretty='format:%C(yellow)%h%Creset %s %Cgreen(%ci) %C(bold blue)<%an>%Creset%C(yellow)%d'
    lgs = log --date=short --pretty='format:%C(yellow)%h%Creset %s %Cgreen(%ci) %C(bold blue)<%an>%Creset%C(yellow)%d' -n9
    lgsu = log --date=short --pretty='format:%C(yellow)%h%Creset %s %Cgreen(%ci) %C(bold blue)<%an>%Creset%C(yellow)%d'
    lg = log --oneline -n9
    lgu = log --oneline
    mf = merge --ff-only
    sb = show-branch
    st = status --short
    sts = status --short
    stl = status
[format]
    signoff = false
[core]
    editor=vim
[color]
    branch=true
[sendemail]
    smtpserver = mailhost
    confirm = auto
[push]
    default = simple
[notes "rewrite"]
	amend = true
	rebase = true
